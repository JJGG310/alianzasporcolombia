.DS_Store
node_modules/
.vercel/
.netlify/
.claude/
*.log

# Binarios compilados del scraper (cada quien construye el suyo)
scraper/bin/
# Navegadores y capturas de Playwright
.playwright-mcp/
ms-playwright/
*.png
# Artefactos de build del sitio en React
web/dist/

# Secretos y estado local de las CLIs de deploy — nunca al repo
.env
.env.local
.env.*.local
.wrangler/
.vercel/
